[project]
name = "forge-cli"
version = "0.2.0"
description = "AI-powered developer CLI"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "typer",
    "rich",
    "ollama",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
